
/**-------------------------------------------------------------------------
 *
 */
const Input = {
  action: 'dosomething',
  payload: {}
}

/** Declare a test session
 * 
 * @params
 *  - 1: {string|object} Information about the session
 *  - 2: (object) Input to run it. (optional)
 *  - 3: Session executing function
 */
test.session( 'Test session description', Input, function( done ){

  /** Listening to emitted events during execution
   * 
   * @params
   *  - 1: event name
   *  - 2: event listener
   */
   
} )
